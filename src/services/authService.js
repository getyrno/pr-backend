// src/services/authService.js

const userModel = require('../models/userModel');

const registerUser = async (userData) => {
    // Ваша бизнес-логика для регистрации пользователя
};

module.exports = {
    registerUser,
};
